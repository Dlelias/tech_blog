{{#if posts.length }}
<section>
  <h2 class="text-white">Post History</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}

</section>
{{/if}}

<button type="submit" class="btn btn-outline-light"><a class="link" href="/dashboard/new">Add New Post</a></button>

